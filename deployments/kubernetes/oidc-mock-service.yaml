kind: Service
apiVersion: v1
metadata:
  name: server-public
  namespace: oidc-mock
spec:
  selector:
      app: server
  ports:
  - protocol: TCP
    port: 6999
    targetPort: 6999
  type: LoadBalancer
